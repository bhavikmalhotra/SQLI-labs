1.) Clone this repository
2.) run the command `sudo docker-compose up`
3.) TO check whether web server and sql server is running run the command `sudo docker ps`
4.) Browse to localhost:8080 to see the challenge
